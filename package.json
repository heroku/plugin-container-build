{
  "name": "@heroku-cli/plugin-local-build",
  "description": "Use this plugin to build and run your app locally as a docker image/container.",
  "version": "0.1.0",
  "author": "Joe Kutner @jkutner",
  "bugs": "https://github.com/heroku/plugin-local-build/issues",
  "dependencies": {
    "@heroku-cli/command": "^8.1.23",
    "@oclif/command": "^1",
    "@oclif/config": "^1",
    "node-fetch": "^2.1.2",
    "tslib": "^1"
  },
  "devDependencies": {
    "@heroku-cli/schema": "^1.0.5",
    "@oclif/dev-cli": "^1",
    "@oclif/plugin-help": "^2",
    "@oclif/test": "^1",
    "@oclif/tslint": "^1",
    "@types/chai": "^4",
    "@types/mocha": "^5",
    "@types/node": "^10",
    "chai": "^4",
    "globby": "^8",
    "mocha": "^5",
    "nyc": "^11",
    "ts-node": "^6",
    "tslint": "^5",
    "typescript": "^2.9"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "files": [
    "/lib",
    "/oclif.manifest.json",
    "/yarn.lock"
  ],
  "homepage": "https://github.com/heroku/plugin-local-build",
  "keywords": [
    "oclif-plugin"
  ],
  "license": "MIT",
  "oclif": {
    "commands": "./lib/commands",
    "bin": "oclif-example",
    "devPlugins": [
      "@oclif/plugin-help"
    ]
  },
  "repository": "heroku/plugin-local-build",
  "scripts": {
    "postinstall": "node scripts/download.js 0.1.4",
    "postpack": "rm -f oclif.manifest.json",
    "posttest": "tsc -p test --noEmit && tslint -p test -t stylish",
    "prepack": "rm -rf lib && tsc && oclif-dev manifest && oclif-dev readme",
    "prepare": "rm -rf lib && tsc",
    "test": "bash bin/test",
    "version": "oclif-dev readme && git add README.md"
  }
}
